//! Implementations for the OpenGL object model. Submodules are re-exported at the top level.

/// Types and implementations related to buffer objects.
pub mod buffer;

/// Types and implementations related to shaders, and shader programs, and uniforms.
pub mod shader;

/// Types and implementations related to vertex specification and vertex array objects.
pub mod vertex;
